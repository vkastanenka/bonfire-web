import { Stack } from "@mui/material";

export const AppSideBar = () => {
  return (
    <Stack
      sx={{
        border: "inherit",
        borderWidth: "1px",
        borderLeftStyle: "solid",
        borderTopStyle: "solid",
        borderColor: "divider",
        bgcolor: "grey.50",
      }}
    >
      AppSideBar
    </Stack>
  );
};
