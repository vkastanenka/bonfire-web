import { Stack } from "@mui/material";

export const AppDashboard = () => {
  return (
    <Stack
      sx={{
        border: "inherit",
        borderWidth: "1px",
        borderTopStyle: "solid",
        borderColor: "divider",
        width: "100%",
      }}
    >
      AppDashboard
    </Stack>
  );
};
