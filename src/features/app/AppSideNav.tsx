import { Stack } from "@mui/material";
import { AppSideNavButton } from "./AppSideNavButton";

export const AppSideNav = () => {
  return (
    <Stack sx={{ bgcolor: "grey.50" }}>
      <AppSideNavButton />
    </Stack>
  );
};
